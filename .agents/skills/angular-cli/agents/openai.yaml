interface:
  display_name: "Angular Cli"
  short_description: "Repo-specific patterns and workflows for angular-cli"
  default_prompt: "Use the angular-cli repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true